# Railway Environment Variables
PORT=3000
RAILWAY_ENVIRONMENT=production

# Dokploy Configuration
DOKPLOY_PORT=3000
ADVERTISE_ADDR=[::]

# Database Configuration (optional - Dokploy includes its own)
DATABASE_URL=

# Redis Configuration (optional - Dokploy includes its own)
REDIS_HOST=

# SSL Configuration
TRAEFIK_SSL_PORT=443
TRAEFIK_PORT=80

# Domain Configuration
# Set these after deployment in Railway dashboard
RAILWAY_PUBLIC_DOMAIN=
RAILWAY_PRIVATE_DOMAIN=